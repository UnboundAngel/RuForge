/** Bottom-right dev-capture toast stack. */
export const DEV_CAPTURE_AUTO_DISMISS_MS = 6000;

/** Crash-preview island saved row (thumb + caption). */
export const ISLAND_CAPTURE_AUTO_DISMISS_MS = 2500;
