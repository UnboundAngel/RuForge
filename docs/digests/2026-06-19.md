# RuForge daily engineering digest

**Window:** 2026-06-18 17:02 UTC through 2026-06-19 17:02 UTC  
**Branch:** `main` (1 commit, direct push by UnboundAngel; no merged PRs in this window)

## Key changes

- **Scrub previews: Windows path correctness** (`785e058`): Thumb subdirs now strip trailing dots/spaces via `thumb_dir_for_stem` so ffmpeg can write sprites for yt-dlp `...webm` titles; ffmpeg sidecar switched from spawn/event loop to `output()` + `-nostdin` so sprite jobs finish instead of hanging; hover cells use `<img>` instead of CSS `background-image` so `#` in download folder names loads correctly (`utils.rs`, `media.rs`, `scrubSpritePreview.tsx`).
- **Shell and chrome polish** (`785e058`): Explorer webview host fills `rf-main-content-shell` (removed stale `fixed top-10` cutout); Video Library scroll shows one left bulge only (no stacked active pill); Win11 snap layout flyout on maximize hover via new `tauri-plugin-snap-layout` (`App.tsx`, `explorerBoundsSync.ts`, `lib.rs`, `Cargo.toml`).
- **Branding and Windows taskbar** (`785e058`): App icon asset renamed to `ruforgeAppIcon.png`; full Tauri icon set regenerated; dev builds use separate AppUserModelID (`.dev`) + HWND `set_icon` so taskbar icon does not stick on stale shell cache (`windows_audio_brand.rs`, `build.rs`).
- **Authorize Cleanup and storage warnings** (`785e058`): Header shows selected-item tally and goal progress bar; audio tracks use listen-snapshot % instead of playbackStorage; sidebar glyph and music storage strip warn at 50% cap (was 80%). New `cleanupCandidates.test.ts` covers tally logic (`AuthorizeCleanupModal.tsx`, `cleanupCandidates.ts`).
- **Gallery and playback edges** (`785e058`): `.temp.mp4` yt-dlp merge intermediates hard-skipped from gallery scan and swept on cleanup; SponsorBlock skip button anchors above player chrome when controls hide; comments panel gets fixed top scroll vignette (`gallery.rs`, `downloader.rs`, `SponsorBlockSkipButton.tsx`, `CommentsPanel.tsx`).
- **Dev tooling** (`785e058`): `verify_gallery_folder` bin added behind `dev-verify-bins` feature flag; `thumb_subdir_name` unit tests in `utils.rs`.

## Watchlist

- **0.1.12 is on `main` but not shipped:** `updater.json` still points at 0.1.11 until signed Windows build + release ritual. This commit is the bulk of the unreleased delta (~91 files).
- **ffmpeg `output()` tradeoff:** Fixes hung sprite jobs but blocks until each encode completes; parallel scrub backfill still runs multiple jobs. Worth watching CPU/disk on large libraries during auto-preview generation.
- **No PR review gate:** Single large direct push touching `gallery.rs`, `media.rs`, `PlayerView.tsx`, and new `tauri-plugin-snap-layout`. Win11 snap flyout and explorer bounds are layout-sensitive; manual smoke on Windows recommended before release.

**Evidence:** `git log origin/main --since="2026-06-18T17:00:00Z" --until="2026-06-19T17:05:00Z"` → `785e058`; `gh pr list --state merged` returned none.
