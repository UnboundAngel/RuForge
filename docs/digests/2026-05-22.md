# RuForge daily engineering digest

**Window:** 2026-05-21 17:02 UTC through 2026-05-22 17:02 UTC  
**Branch:** `main` (8 commits, all direct pushes by UnboundAngel; no merged PRs in this window)

## Key changes

- **v0.1.7 shipped to auto-update users** (`617cf3d`): Triplet bumped to 0.1.7, `updater.json` signed for NSIS, `docs/versions/version-0.1.7.json` and versioner registry added. Live `main` updater still reports **0.1.7** (verified 2026-05-22).
- **Player and playback** (`1b6fae4`, `b24be5e`): Chapter scrubber from yt-dlp sidecars, frosted control dock, audio-only hero with 90-bar LED equalizer (`AudioHeroStage.tsx`, `audioAnalyserGraph.ts`), SponsorBlock colors extended to audio and mini layouts, mini player tooltip/volume fixes.
- **SponsorBlock + library correctness** (`d4dd461`): New Rust `sponsorblock.rs`, privacy-hash fetch, sidecar cache, scrub overlays and Playback settings tree; gallery dedupe for muxed vs `.fNNN` leftovers and cross-folder merge (`gallery.rs`, `galleryDedupe.ts`).
- **Explorer dependency trim** (`6c0ea5f`, `d4dd461`): Removed bundled uBlock tree from git and wiring; path now `.gitignore`d. Aligns with AGENTS.md treating in-app ad blocking as experimental, not relied on.
- **Public site + 0.1.8 polish** (`42d2184`): New Astro 5 `website/` (home, changelog, roadmap, legal via `docs/legal/*.md`), optimized images, landing/testimonials rework; app-side mini player blur wash fix and updater post-install scroll clamp. **Still unreleased** (version triplet and `updater.json` remain 0.1.7).
- **Repo hygiene** (`658daea`, `d56e25c`): Apache License 2.0 added; README capitalization and grammar pass.

## Watchlist

- **0.1.8 delta is on `main` but not on the updater path** until signed Windows build, `updater.json` bump, and release ritual. Users on 0.1.7 do not receive website or latest mini/updater fixes yet.
- **High-touch surfaces without PR review gate:** ~10k LOC touched across `gallery.rs`, `PlayerView.tsx`, `MiniPlayer.tsx`, and new SponsorBlock/Web Audio paths. WebView2 CORS and `captureStream()` analyser taps are regression-prone.
- **SponsorBlock default-on** calls third-party API on playback; silent fail is intentional but worth monitoring for privacy and offline behavior.

**Evidence:** `git log origin/main --since="2026-05-21T17:00:00Z" --until="2026-05-22T17:05:00Z"`; `gh pr list --state merged` returned none.
