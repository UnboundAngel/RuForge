---
version: "0.2.0"
date: 2026-06-28
additions:
  - "Music playlists: batch downloads write `.ruforge-playlist.json` under Playlists with per-track roster and downloading/complete/incomplete status; Explore harvest path skips yt-dlp when webview roster is complete"
  - "Music Explore: SPA page context via History API in embedded WebView2; YTM header title polling; signed playlist coverUrl on album view with lazy sidecar heal"
  - "Music download dock: Ban cancel on expanded chip and collapsed orb; sidebar orbs for all active jobs even when panel never opened; cancel-all in panel header and collapsed stack"
  - "Music library: artist view immersive hero; canonical_album prefers yt-dlp info.json; album grouping strips date stamps and venue fallback; metadata backfill force re-process checkbox"
  - "Downloads: multi-item carousel hero (centered cards, plain progress, MB/s on active row); batch UI persists through playlist and Explorer multi-add; batch focus follows actively downloading job"
  - "Downloads: Deno JS runtime auto-install when yt-dlp reports no supported runtime; Settings Downloads shows status and install button; failed jobs in batch re-queue after install"
  - "Downloads: music batch retry-at-end (up to 3 attempts per failed track); Explorer mirrorHeroUrl and session queue restore on tab change"
  - "Scrub previews: parallel ffmpeg generation, global fleet cap, tail cell patch to duration; windowed sprite sheet preload on long videos; library backfill when auto mode on"
  - "Debugging: dev captures library under Settings Debugging (native screenshot, annotate, grid, drag-out); replay last download batch (Mode A real, Mode B simulate)"
  - "Crash recovery: root error boundary on every window; friendly hero with Reload app and collapsible details"
  - "Telemetry: Aptabase gated behind showDebuggingSettings; dev-gate.json boot mirror; public consent overlay removed"
  - "Authorize Cleanup: audio tracks use listen-snapshot percent; header item tally and goal progress bar; storage glyph warns at 50% cap"
  - "Window chrome: Win11 snap layout flyout on maximize hover; Explorer webview fills content shell below titlebar"
  - "Comments panel: thin fixed top scroll vignette when threads scroll"
fixes:
  - "Music: playlist-in-library badge reads sidecar status complete by listUrl; stale bare-maxres coverUrl treated as empty"
  - "Music: manual download cancel shows red cancel celebration instead of green success; expanded cancel hold ring timing fixed"
  - "Music: artist context menu no longer crashes when display name lacks feat segment"
  - "Downloads: watchdog pre-transfer clock arms after yt-dlp spawn not before simulate"
  - "Downloads: stranded yt-dlp .temp.mp4 hard-skipped from gallery and swept on cleanup"
  - "Downloads: queue metadata hydration capped at 2 concurrent get_video_info; URL dedup uses normalizeYouTubeUrlForCompare"
  - "Scrub previews: Windows thumb subdirs strip trailing dots; hover thumb uses img not CSS url for # in paths; ffmpeg sidecar uses output() with -nostdin"
  - "Scrub hover: rAF-throttled hover; sprite paths cached at gallery scan; defer library backfill during playback"
  - "Windows taskbar: dev builds use separate AppUserModelID and HWND set_icon to avoid stale shell icon"
  - "SponsorBlock: skip button tracks player chrome height when controls hide"
  - "Video Library: scroll bulge only; active pill hidden while bulge visible"
  - "Media chrome: tab bulge fillets track --rf-titlebar-h on Video Library and Settings"
  - "Debugging: dev capture and crash preview resolve main window via boot-cached state not child webview invoke context"
---
