---
version: "0.1.10"
date: 2026-06-12
additions:
  - "Website release automation: site version from root package.json at build time; npm run prep:website-release generates changelog from updater.json and copies signed NSIS for same-origin download"
  - "Listen-event log: append-only track_played JSONL in app data; dual-surface session coordinator (main + music-mini) with handoff; listen stats and play history from Rust snapshot; legacy localStorage import; music_listen_clear command"
  - "Music profile screen: YouTube identity, glance stats, highlights, liked songs, recent plays, storage; titlebar chip opens profile; Home stats strip removed in favor of profile entry"
  - "Music storage strip: bottom library storage bar on Home and Library (used GB, cap progress on internal vault); hides during playback, Explore bar, active downloads, or expanded player"
  - "YouTube titlebar profile chip: Log in pill when signed out; avatar-ratio spinner until PFP loads; session cached in localStorage; chip in titlebar across all modes; hover expands to @handle"
fixes:
  - "Music Explore auto-save: queue download only after 15s on the same track; skip cancels pending timer for the previous song"
  - "Download complete animation: global job-completion celebrations (auto-save and pick tracks); green/yellow ring and checkmark from first completion frame; auto-save thumbs on enqueue"
  - "Download watchdog: progress lines that do not advance bytes or percent no longer reset idle; audio-only stalls yellow-timeout; 30s pre-transfer, 45s audio idle, 2m wall clock from downloadingSince; re-arms after sleep, HMR, or foreground; timed_out status advances batch with yellow warning on Explore orb"
  - "Listen log integrity: accumulate uses deltas; end closes with server total only; v2 JSONL events; cutover in music-listen-integrity.json; scoring gates pre-cutover listenTimeSec"
  - "Music mini: clean transparent window corners; skip next/prev preserves play state"
  - "Music stats and profile UI: anti-pattern visual pass; plain labels; top track time and plays on cover hover only; responsive Home Quick picks grid; Liked Songs full-width banner row"
  - "Storage glyph: sidebar storage ring opens Authorize Cleanup on click (matches Music storage strip)"
  - "YouTube profile @handle: probe scoped to topbar and account menu only; brief account menu open to read real handle from DOM; hover shows @handle not Your channel"
---
