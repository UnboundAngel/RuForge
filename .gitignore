# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
target/
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri
src-tauri/target/

# Environment
.env
.env.*
!.env.example

# Private Discord bot (local only, not shipped)
ruforge-discord-bot/

# Local-only Tauri signing helper (optional one-line password for scripts/build-signed-windows.ps1)
.tauri-signing-password

# IDE / local plans (shared agent rules under .cursor/rules/; project skills under .cursor/skills/)
.cursor/*
!.cursor/rules/
!.cursor/rules/**
!.cursor/skills/
!.cursor/skills/**

# Removed bundled browser extension (do not re-add)
src-tauri/extensions/ublock/

# Local History extension snapshots
.history/

# Agent scratch copies of updater.json
_live-updater-check*.json

# Unreleased log scratch (written by agents, consumed by scripts/shipped.mjs)
.shipped-entry.txt

# Local chat tone (not for GitHub)
AGENTS.local.md

# Ad-hoc archives and large optional FFmpeg binaries (see AGENTS.md; do not commit multi-hundred-MB exes)
ffmpeg.7z
src-tauri/binaries/ffmpeg*.exe
src-tauri/binaries/ffprobe*.exe
